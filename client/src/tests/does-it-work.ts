import { strictEqual } from "assert";

describe("Mocha", () => {
    it("should be able to execute a test", () => {
      strictEqual(true, true);
    });
    // it("should fail", () => {
    //     strictEqual(13,24)
    // });
});